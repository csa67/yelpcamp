<% layout('Boilerplate') -%>
<h1> All campgrounds </h1>
 <% for(let camp of campgrounds) {%>
 <div class="card mb-3">
    <div class="row">
        <div class="col-md-4">
            <img class="img-fluid" alt="" src=<%= camp.image%>>
        </div>
        <div class="col-md-8">
        <div class="card-body">
            <h5 class="card-title"><%= camp.title%> </h5>
            <p class="card-text"><%= camp.desc %> </p>
            <p class="card-text"> <small class="text-muted"> <%= camp.loc %> </small></p>
            <a href="/campgrounds/<%= camp._id%>" class="btn btn-primary">View Details</a>
        </div>
        </div>
    </div>
</div>
<% }%>
</ul>    